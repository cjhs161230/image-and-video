# 需求说明

本项目是 Windows 本机单用户图像与逐帧视频工作台，只监听 `127.0.0.1`。

核心需求：

- 图片任务进入持久化队列，支持暂停、继续、取消和重启恢复。
- 视频流程依次经过草稿、分镜确认、关键帧确认、连续帧、合成。
- 分镜和关键帧完成后必须暂停，等待用户确认。
- Matsca 并发采用保守值：应用 1、直连 5、原生 50，并从 1 渐进升档。
- 密钥只从 `.env` 读取，普通设置写入 `data/settings.json`。
- 运行数据、日志、上传、媒体、迁移数据全部位于 Git 忽略的 `data/`。
